spring:
  application:
    name: portholemap-api
  mvc:
    servlet:
      path: /api

management:
  server:
    port: 8081

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
